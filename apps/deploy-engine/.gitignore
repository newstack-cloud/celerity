# Code generated pre-build, (e.g genny generics templates)
gen-*.go

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out
coverage.txt
coverage.html

# Test report for sonar.
report.json

# Dependency directories (remove the comment below to include it)
# vendor/

# Any log files.
*.log

.env

# Directory for postgres migration files copied from the
# blueprint-state library when running the deploy engine locally.
postgres

# Test blueprint files.
*.blueprint.yml
*.blueprint.json

# The core.versions.go file is generated, usually during the
# build process to package version information into the binary.
core/versions.go

tmp
